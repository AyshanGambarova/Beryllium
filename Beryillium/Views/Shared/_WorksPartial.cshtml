@model List<Work>

@foreach (Work item in Model)
{
    <div class="item col-md-4">
        <div class="gtco-item">
            <a href="/Search/Index"><img src="~/images/@item.Img" alt="" class="img-responsive"></a>
            <h2><a href="#">@item.Subtitle</a></h2>
            <p class="role">@item.Title</p>
        </div>
    </div>
 

}
